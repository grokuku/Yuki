# Yuki — Lots 0-1 : socle, porte de compatibilité & noyau texte
# Le service `gateway` est déclaré ici ; le service `tts` (Lot 7, audio.cpp
# CUDA) est OPT-IN derrière le profil Compose `tts`
# (`docker compose --profile tts up -d`) et n'est donc pas démarré par défaut.
# Pas de clé `version:` (obsolète en Compose v5).
#
# Ce compose TIRE l'image publiée (`ghcr.io/grokuku/yuki`) : `docker compose up -d`
# suffit, sans construire ni préparer l'hôte. La persistance passe par 5 VOLUMES
# NOMMÉS (plus de bind mounts) — voir la section `volumes:` du service.
#
# Pour construire en local : `compose.build.example.yml`.
# Pour revenir à des bind mounts : `compose.bind.example.yml`.
# Aucun tag flottant : l'image Node du Dockerfile est épinglée par digest.

name: yuki

services:
  gateway:
    # Image publiée sur ghcr.io (références EN MINUSCULES). Construite par la CI :
    # `test-build.yml` (`:test`) et `release.yml` (`:latest` / `:<version>`).
    # Le compose de BASE tire l'image ; pour construire en local, voir
    # `compose.build.example.yml`.
    image: ghcr.io/grokuku/yuki:${YUKI_VERSION:-latest}
    container_name: yuki-gateway

    env_file:
      - .env
    environment:
      NODE_ENV: production
      YUKI_VERSION: ${YUKI_VERSION:-0.1.0}
      YUKI_GATEWAY_PORT: "8080"
      YUKI_GATEWAY_HOST: 0.0.0.0
      # `YUKI_GPU_CMD` n'est PAS défini ici : sinon le code croirait toujours que
      # la variable a été fournie (`source` = `env-override`). Le défaut
      # (`nvidia-smi`) est appliqué par le code ; la surcharge reste possible
      # via `.env` (chargé par `env_file`).
      #
      # ⚠️ AUCUN chemin INTERNE au conteneur n'est défini ici (cibles `/models`,
      # `/data/tts-config`, `/config`, `/voices`, `/data/pi`, `/workspace`,
      # `/data/state` et chemins Pi dérivés). Ce sont des DÉFAUTS DU CODE
      # (`src/config/container-paths.ts`), EXACTEMENT égaux aux cibles des
      # `volumes:` ci-dessous. Le compose reste muet à leur sujet ; la surcharge
      # par variable reste lue (rétro-compatibilité). Voir `docs/lot9.md` (D61).

      # --- Environnement SDK Pi (coupure des opérations réseau au démarrage) ---
      # Non-chemin, redondant avec `applyPiEnvironment` (`src/pi/config.ts`).
      PI_OFFLINE: "1"
      PI_SKIP_VERSION_CHECK: "1"
      PI_TELEMETRY: "0"

      # Injecte `nvidia-smi` dans le conteneur -> preuve du passthrough GPU.
      NVIDIA_VISIBLE_DEVICES: all
      NVIDIA_DRIVER_CAPABILITIES: compute,utility

    # Exécution non-root. ⚠️ L'image TIRÉE est construite avec uid/gid 1000 (voir
    # `ARG YUKI_UID/YUKI_GID` dans le Dockerfile). Changer `YUKI_UID`/`YUKI_GID`
    # dans `.env` SANS reconstruire l'image ferait tourner le conteneur avec un
    # uid différent de celui du volume → erreurs de permissions. Ne les changer
    # que si l'on reconstruit l'image (`compose.build.example.yml`) avec les
    # mêmes valeurs.
    user: "${YUKI_UID:-1000}:${YUKI_GID:-1000}"

    ports:
      - "${YUKI_GATEWAY_PORT:-8080}:8080"

    # 5 VOLUMES NOMMÉS (et non des bind mounts). Pour un volume nommé monté sur
    # un chemin qui EXISTE déjà dans l'image, Docker initialise le volume en
    # recopiant le CONTENU et le PROPRIÉTAIRE du répertoire de l'image (créé en
    # `YUKI_UID:YUKI_GID` par le Dockerfile). Le conteneur non-root écrit donc
    # sans aucune préparation sur l'hôte — `docker compose up -d` suffit.
    volumes:
      - type: volume
        source: yuki-pi
        target: /data/pi
      - type: volume
        source: yuki-workspace
        target: /workspace
      # Lot 9 (M1) : UN SEUL montage `/models`, en `rw` côté gateway (lecture +
      # écriture FUTURE des téléchargements). Le sous-dossier `downloads/` est
      # une CONVENTION d'organisation (les GGUF téléchargés y sont rangés), pas
      # une barrière : le gateway peut écrire partout sous `/models`.
      # ⚠️ Le montage du MOTEUR (service `tts`, plus bas) reste `ro`.
      - type: volume
        source: yuki-models
        target: /models
      # Lot 9 (M2) : dossier de configuration du moteur, monté en `rw` pour que
      # le gateway écrive `server.json` ATOMIQUEMENT (un `rename` est impossible
      # sur un fichier bind-monté, d'où le dossier). Le moteur le monte en `ro`
      # (M3), et la commande du moteur reste `server --config /config/server.json`.
      - type: volume
        source: yuki-tts-config
        target: /data/tts-config
      - type: volume
        source: yuki-state
        target: /data/state
      # Lot 7 : registre + échantillons de voix (rw pour le gateway, ro pour `tts`).
      - type: volume
        source: yuki-voices
        target: /voices

    networks:
      - yuki-net

    restart: unless-stopped
    init: true

    # Rootfs immuable ; seul /tmp est inscriptible (tmpfs).
    read_only: true
    tmpfs:
      - /tmp

    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: "3"

    # Healthcheck sans curl : le binaire Node embarque `fetch`.
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:8080/health/live').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 20s

    # Réservation GPU (forme requise par le NVIDIA Container Toolkit).
    # `capabilities` est obligatoire.
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

  # ===========================================================================
  # Service TTS (Lot 7) — audio.cpp (Chatterbox Multilingual V3, CUDA NVIDIA).
  #
  # OPT-IN : derrière le profil Compose `tts`, il n'est PAS démarré par
  # `docker compose up -d` (image CUDA volumineuse, GPU requis). Pour l'activer :
  #
  #     docker compose --profile tts up -d
  #
  # Variante CUDA : l'image se choisit via `YUKI_TTS_IMAGE` dans `.env`
  # (`full-cuda13` par défaut, `full-cuda12` si le driver l'impose).
  #
  # ⚠️ COMMANDE (spec §13/C14) — corrigée d'après une EXÉCUTION RÉELLE :
  # l'ENTRYPOINT de l'image est un DISPATCHER à sous-commandes
  # (`cli`, `server`, `model-manager`, `perf`). Passer `--config` en 1er argument
  # échoue avec « Unknown command: --config » (voir `docs/lot8.md` §11.4).
  # Le 1er argument doit donc être la sous-commande `server`.
  #
  # Hôte/port ne sont PAS des flags : ce sont des CLÉS DU FICHIER DE CONFIG
  # (`host`/`port`), d'où l'absence de `--host`/`--port` ci-dessous.
  #
  # ⚠️ Le chemin `/config/server.json` est un choix Yuki (le WORKDIR de l'image
  # n'est pas attesté) ; il est cohérent entre le compose (M3), le gateway et
  # l'interface. C'est un DÉFAUT DU CODE : ne définissez PAS
  # `YUKI_TTS_ENGINE_CONFIG_DIR` ici. La surcharge reste possible (relocalisation
  # avancée) mais n'est PAS un réglage du compose (voir `docs/lot9.md` D61).
  # ===========================================================================
  tts:
    image: ${YUKI_TTS_IMAGE:-ghcr.io/0xshug0/audio.cpp:full-cuda13}
    container_name: yuki-tts
    profiles: ["tts"]
    # Dispatcher audio.cpp → sous-commande `server`, puis `--config <fichier>`.
    # Le serveur écoute sur `host:port` du fichier de config (0.0.0.0:8081),
    # aligné sur le défaut `tts.baseUrl` = http://tts:8081.
    # Lot 9 (M3) : le DOSSIER de config est monté en `ro` sur `/config` ; la
    # commande NE change plus (`server --config /config/server.json`).
    command: ["server", "--config", "/config/server.json"]
    networks:
      - yuki-net
    volumes:
      # Lot 9 (M3) : dossier de configuration du serveur audio.cpp, monté en
      # `ro`. Le MÊME dossier est monté en `rw` côté gateway (M2) → l'édition
      # structurée depuis l'interface Yuki écrit `server.json` ici.
      # ⚠️ Le moteur ne reçoit JAMAIS de montage `rw` : il relit le fichier à
      # son redémarrage.
      - type: volume
        source: yuki-tts-config
        target: /config
        read_only: true
      # Modèle GGUF (ro) — jamais écrit par le service (§11.6).
      - type: volume
        source: yuki-models
        target: /models
        read_only: true
      # Voix : le service ne fait que LIRE les échantillons du registre Yuki.
      - type: volume
        source: yuki-voices
        target: /voices
        read_only: true
    # Réservation GPU (même forme que le gateway).
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    restart: unless-stopped
    # Rootfs immuable ; /tmp inscriptible (tmpfs).
    read_only: true
    tmpfs:
      - /tmp
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: "3"

networks:
  yuki-net:
    name: yuki-net

# Noms de volumes EXPLICITES et stables (sans préfixe de projet) : faciles à
# sauvegarder, inspecter et restaurer avec `docker volume …`. Voir docs/runbook.md.
volumes:
  yuki-pi:
    name: yuki-pi
  yuki-workspace:
    name: yuki-workspace
  yuki-models:
    name: yuki-models
  yuki-state:
    name: yuki-state
  # Lot 7 : médias de voix (registre `voices.json` + WAV) — volume dédié.
  yuki-voices:
    name: yuki-voices
  # Lot 9 : dossier de configuration du moteur `audio.cpp` (`server.json` +
  # `server.json.bak`). Monté `rw` côté gateway, `ro` côté moteur.
  yuki-tts-config:
    name: yuki-tts-config
